# test ?
server:
  port: 8080

spring:
  application:
    name: catalog-server
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    hikari:
      max-lifetime: 160000

  jpa:
    defer-datasource-initialization: false
    generate-ddl: false

    hibernate:
      ddl-auto: none
    open-in-view: false
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.MySQLDialect

cognito:
  iss: "https://cognito-idp.ap-northeast-2.amazonaws.com/ap-northeast-2_584en6hOW/.well-known"

openai:
#  key: ${OPENAI_KEY}
  key: sk-proj-p2ti3uV1OWTRF3d5IljCT3BlbkFJGUHpeTHBINuJDMP5x5l3
pinecone:
  key: ${PINECONE_KEY}
  env:
    qna: aped-4627-b74a
#    qna: ${PINECONE_ENV_QNA}
  id:
#    qna: ${PINECONE_ID_QNA}
    qna: kyr8e0r

